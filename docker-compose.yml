services:
  rapport-cot:
    build: .
    container_name: rapport-cot
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    command: streamlit run comparator.py --server.address=0.0.0.0 --server.port=8501
